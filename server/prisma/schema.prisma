generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model AdminUser {
  id        String   @id @default(uuid())
  username  String   @unique
  password  String
  createdAt DateTime @default(now())
}

model Project {
  id              String   @id @default(uuid())
  number          String
  name            String
  type            String
  description     String
  longDescription String?
  technologies    String   // JSON string of string[]
  images          String   // JSON string of string[]
  metrics         String?  // JSON string of {label: string, value: string}[]
  liveUrl         String?
  featured        Boolean  @default(false)
  sortOrder       Int      @default(0)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model Moment {
  id          String   @id @default(uuid())
  title       String
  category    String   // WORK, HOMETOWN, TRAVEL, LIFESTYLE
  location    String?
  aspectRatio String   @default("landscape") // portrait, landscape, square, wide
  image       String
  caption     String
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())
}

model JourneyStep {
  id          String   @id @default(uuid())
  number      String
  stage       String
  period      String
  title       String
  description String
  tags        String   // JSON string of string[]
  sortOrder   Int      @default(0)
}

model ContactMessage {
  id          String   @id @default(uuid())
  name        String
  contactInfo String
  message     String
  isRead      Boolean  @default(false)
  createdAt   DateTime @default(now())
}

model SiteSetting {
  key       String   @id
  value     String   // JSON string
  updatedAt DateTime @updatedAt
}

model Service {
  id          String   @id @default(uuid())
  category    String
  icon        String   @default("Code")
  accentColor String   @default("#00D2FF")
  title       String
  tagline     String
  features    String   // JSON string of string[]
  ctaUrl      String   @default("https://t.me/trungluanmmo")
  ctaText     String   @default("Tư vấn ngay")
  highlight   Boolean  @default(false)
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

